[defaults]
# 保留首次连接主机指纹校验，避免中间人攻击。
host_key_checking = True

# 统一实现的角色在 ansible/roles，供从仓库根运行的工具（如 ansible-lint）解析短角色名。
roles_path = ansible/roles

# 设置并发数
forks = 5

# 超时设置
timeout = 30

# 日志级别
verbosity = 0

# 启用密码提示（如果需要）
ask_pass = False
ask_become_pass = False

# SSH 连接设置
[ssh_connection]
# SSH 超时
ssh_timeout = 10

# 连接参数
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
