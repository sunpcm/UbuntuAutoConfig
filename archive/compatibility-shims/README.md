# 已移除的兼容入口

本目录保存 2026-07-27 前位于仓库根目录的兼容入口：

- `playbook.yml`
- `setup_wsl.yml`
- `host.ini.examples`

它们只用于追溯旧命令如何转发到统一实现，已不再受支持。不要从本目录执行这些文件；相对路径在归档后
也不保证有效。当前入口见仓库根目录 [`README.md`](../../README.md) 的 `bin/` 命令和交互式向导。
