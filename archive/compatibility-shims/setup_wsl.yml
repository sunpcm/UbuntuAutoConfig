---
# Deprecated compatibility entrypoint. Prefer: bin/wsl-bootstrap <target_user>
- import_playbook: ansible/playbooks/wsl-bootstrap.yml
  vars:
    target_user: "{{ ansible_new_user | default(lookup('env', 'TARGET_USER'), true) }}"
